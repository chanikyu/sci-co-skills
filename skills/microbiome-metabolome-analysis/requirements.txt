# Pure-Python metabolomics statistics (see environment.yml for the conda env). Python deps:
numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
scikit-learn>=1.3
matplotlib>=3.8
# optional: gseapy>=1.1   # ORA enrichment, only when metabolites are mapped to KEGG/HMDB IDs
# Reuses sibling skill scientific-data-viz for figures.
