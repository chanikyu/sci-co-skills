# transcriptome-analysis uses conda (see environment.yml) — the quantifiers (salmon, kallisto,
# STAR, subread) are not on PyPI. Python-only deps for the DE + figures downstream:
pydeseq2>=0.4
gseapy>=1.1
matplotlib>=3.8
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
# Reuses sibling skill scientific-data-viz for figures.
