# resistome-analysis uses conda (see environment.yml) — the ARG tools (DeepARG, RGI, hAMRonization)
# are not cleanly on PyPI. Python-only deps for the testable core + reused downstream:
scikit-bio>=0.6
matplotlib>=3.8
numpy>=1.24
scipy>=1.10
pandas>=2.0
# normalization (normalize.py) + drug-class aggregation (aggregate.py) are pure Python (no extra deps).
# Reuses sibling skills amplicon-analysis (diversity + differential core) + scientific-data-viz (figures).
