# virome-analysis uses conda (see environment.yml) — the tools (geNomad, CheckV, CoverM, blast,
# bowtie2, seqkit) are not on PyPI. Python-only deps for the reused downstream core + glue:
scikit-bio>=0.6
matplotlib>=3.8
numpy>=1.24
scipy>=1.10
pandas>=2.0
# dereplication (derep.py) + abundance assembly (abundance.py) are pure Python (no extra deps).
# Reuses sibling skills amplicon-analysis (diversity + differential core) + scientific-data-viz (figures).
